# Technical Audit — hairsalonubud.com

> **In plain terms (for the team):** Your site is generally in good shape for search performance: it's secure (HTTPS), Google can crawl it effectively (robots.txt, sitemap), and it's fast thanks to caching and modern protocols. The single biggest risk is the unusual WordPress 7.0 version reported, which needs investigation to ensure your platform is current and secure, preventing potential vulnerabilities or performance issues.

**Server:** hostinger · **Audited:** 2026-06-11 · **Status:** live

## Verified signals
- HTTP/redirects: The site resolves to `https://hairsalonubud.com/` with a 200 OK status.
- robots.txt: Present, allows all user agents (`Disallow:` empty), and correctly references `https://hairsalonubud.com/sitemap_index.xml`.
- sitemap: `sitemap_index.xml` is present, accessible (HTTP 200), and generated by Yoast SEO. It includes `page-sitemap.xml`, `sln_service-sitemap.xml`, and `sln_service_category-sitemap.xml`.
- HTTPS/headers: HTTP/2 protocol is used. `content-type: text/html; charset=UTF-8`. `content-security-policy: upgrade-insecure-requests` header is present. `alt-svc: h3=":443"` indicates HTTP/3 support.
- Platform/version: WordPress 7.0 is reported as the generator. PHP/8.1.34 is the server-side language version.
- Plugins/theme/security: Yoast SEO is used for sitemap generation. LiteSpeed Cache is active (`x-litespeed-cache`). Hostinger CDN (`hcdn`) is active. `content-security-policy: upgrade-insecure-requests` is a security header.

## Technical findings
| Area | Finding | Severity |
|---|---|---|
| Platform Version | WordPress 7.0 is reported as the generator version. This is an unusual version number, as the current stable release series is 6.x. This may indicate a custom build, a misconfigured `generator` meta tag, or a potentially outdated system if it's a typo for an older version. | Med |
| PHP Version | The site is running on PHP/8.1.34, which is a modern and supported version, offering good performance and security. | Info |
| Caching | LiteSpeed Cache and Hostinger CDN are active, indicating effective caching mechanisms are in place to improve site performance. | Info |
| HTTPS Enforcement | The site consistently resolves to HTTPS, and the `upgrade-insecure-requests` CSP header helps ensure all content is served securely. | Info |
| Robots.txt Configuration | The `robots.txt` file is correctly configured to allow crawling of all content and points to the XML sitemap. | Info |
| Sitemap Configuration | A comprehensive XML sitemap index, generated by Yoast SEO, is present and accessible, aiding search engine discovery of content. | Info |
| Schema Markup | JSON-LD schema markup is present on the homepage, which can help search engines understand the content better. | Info |
| Modern Protocols | Support for HTTP/2 and HTTP/3 (via `alt-svc`) is enabled, leveraging modern web protocols for faster content delivery. | Info |
| Server Response Time | The upstream response time from the Hostinger CDN is fast (74ms), contributing to overall site speed. | Info |

## Could not verify
- Plugin/theme conflicts or bloat
- Security: table prefix, debug mode status, exposed files, authentication vulnerabilities
- Core Web Vitals specific scores
- Google Search Console verification status or indexability status

## Top technical fixes (analysis only — NOT executed)
1. Investigate the reported WordPress 7.0 version to confirm its accuracy and ensure the platform is up-to-date and securely maintained.