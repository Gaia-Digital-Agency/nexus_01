# Technical Audit — nusapenida.org
**Server:** hostinger · **Audited:** 2026-06-11 · **Status:** redirect:https://nusapenida.org/nusa-penida-blog-guide/

## Verified signals
- HTTP/redirects:
  - HTTP/2 protocol is in use.
  - The root domain `nusapenida.org` issues a 301 redirect to `https://nusapenida.org/nusa-penida-blog-guide/`.
  - The redirect is managed by Yoast SEO Premium.
  - The final destination `https://nusapenida.org/nusa-penida-blog-guide/` returns a 200 OK status.
  - HTTPS is enforced with a `strict-transport-security` header including `preload`.
- robots.txt:
  - `robots.txt` is present and accessible.
  - It allows all user-agents (`Disallow:` is empty).
  - It declares the sitemap at `https://nusapenida.org/sitemap_index.xml`.
  - It is managed by Yoast SEO.
- sitemap:
  - `sitemap_index.xml` is present, accessible (HTTP 200), and correctly formatted.
  - It includes `post-sitemap.xml` and `page-sitemap.xml` with recent `lastmod` dates (2026-06-09 and 2026-06-11).
  - It is generated by Yoast SEO.
- HTTPS/headers:
  - `strict-transport-security: max-age=63072000; includeSubDomains;preload` is present.
  - `x-xss-protection: 0` is present.
  - `referrer-policy: strict-origin-when-cross-origin` is present.
  - `content-security-policy: upgrade-insecure-requests` is present.
  - `content-security-policy-report-only` with a `report-uri` to `https://nusapenida.org/wp-json/rsssl/v1/csp?rsssl_apitoken=551728966` is present.
- Platform/version:
  - Hosting provider is Hostinger, using hPanel.
  - Server is `hcdn` (Hostinger CDN).
  - CMS is WordPress (indicated by `wp-json`, `xmlrpc.php`, `wp-content/plugins/wordpress-seo`).
  - No WordPress version is exposed via a `generator` meta tag.
- Plugins/theme/security:
  - Yoast SEO Premium plugin is active (managing redirects, robots.txt, sitemap).
  - LiteSpeed Cache plugin is active (`x-litespeed-cache: hit`).
  - Really Simple SSL plugin is likely active (indicated by CSP `report-uri`).
  - `xmlrpc.php` endpoint is exposed.
  - JSON-LD schema is present.

## Technical findings
| Area | Finding | Severity |
|---|---|---|
| HTTP/redirects | The root domain `nusapenida.org` 301 redirects to a specific blog post (`https://nusapenida.org/nusa-penida-blog-guide/`) instead of a general homepage. This may not be the intended canonical URL for the main domain. | Med |
| Security | The `xmlrpc.php` endpoint is exposed, which is a common target for brute-force attacks and other vulnerabilities on WordPress sites. | Med |
| On-page structure | The page `https://nusapenida.org/nusa-penida-blog-guide/` (acting as the primary landing page) lacks an `<h1>` heading. | Med |
| Security | The `x-xss-protection` header is set to `0`, effectively disabling XSS protection in browsers that still support it. | Low |
| Performance | HTTP/3 (QUIC) support is enabled (`alt-svc: h3=":443"`). | Info |
| Performance | Hostinger CDN (`hcdn`) and LiteSpeed Cache are active and serving cached content. | Info |
| Security | A comprehensive Content Security Policy (CSP) is implemented with `upgrade-insecure-requests` and a `report-only` mode, indicating proactive security measures. | Info |

## Could not verify
- Specific WordPress version number.
- Plugin/theme stack conflicts or bloat (beyond identifying active plugins).
- Security: database table prefix, debug mode status, other exposed sensitive files (beyond `xmlrpc.php`), authentication methods.
- Detailed speed metrics or Core Web Vitals scores.
- Google Search Console verification status.
- Specific JSON-LD schema types.

## Top technical fixes (analysis only — NOT executed)
1. Review and adjust the 301 redirect from the root domain (`nusapenida.org`) to ensure it points to the intended canonical homepage, if `nusa-penida-blog-guide/` is not meant to be the primary entry point.
2. Disable or restrict access to `xmlrpc.php` to enhance security and prevent potential attacks.
3. Add a primary `<h1>` heading to the `https://nusapenida.org/nusa-penida-blog-guide/` page for improved content structure and accessibility.