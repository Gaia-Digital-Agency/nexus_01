# Technical Audit — scamcheck-global.com

> **In plain terms (for the team):** Your site is secure (HTTPS) and Google can fully crawl it (robots.txt, sitemap), but its speed could be significantly improved by optimizing how your content is cached (CDN cache status is DYNAMIC). The single biggest risk is the exposed WordPress REST API endpoints, which could be exploited without proper security measures.

**Server:** hostinger · **Audited:** 2026-06-11 · **Status:** live

## Verified signals
- HTTP/redirects: Site resolves to `https://scamcheck-global.com/` with a 200 OK status.
- robots.txt: `User-agent: *` with `Disallow:` (empty, allowing all user agents). Sitemap linked: `https://scamcheck-global.com/sitemap_index.xml`. Generated by Yoast SEO.
- sitemap: `sitemap_index.xml` is present, returns HTTP 200, and lists `post-sitemap.xml` and `page-sitemap.xml`. Generated by Yoast SEO.
- HTTPS/headers: Site serves over HTTPS (HTTP/2) with a 200 OK status. `Content-Security-Policy: upgrade-insecure-requests` header is present. Supports HTTP/3 (alt-svc: h3).
- Platform/version: WordPress 6.9.4 detected, running on PHP 8.3.30. Hosted on Hostinger (hpanel).
- Plugins/theme/security: Yoast SEO and Site Kit by Google plugins detected. WordPress REST API endpoints (`wp-json`) are exposed. `Content-Security-Policy: upgrade-insecure-requests` header is present.

## Technical findings
| Area | Finding | Severity |
|---|---|---|
| Hosting/Server | Site hosted on Hostinger with HPanel and Hostinger CDN (hcdn). | Info |
| Performance | Site serves over HTTP/2 and supports HTTP/3 (alt-svc: h3). | Info |
| Performance | CDN cache status is DYNAMIC, which may indicate non-optimal caching for static assets. | Low |
| Platform | WordPress 6.9.4 detected. | Info |
| Platform | PHP 8.3.30 detected. | Info |
| Security | `Content-Security-Policy: upgrade-insecure-requests` header is present. | Info |
| Security | WordPress REST API endpoints (`wp-json`) are exposed. | Info |
| Indexability | `robots.txt` allows all user agents. | Info |
| Indexability | XML sitemap (`sitemap_index.xml`) is present, linked in `robots.txt`, and returns 200 OK. | Info |
| Indexability | JSON-LD schema is present on the homepage. | Info |

## Could not verify
- Specific WordPress theme used.
- Plugin/theme stack conflicts or bloat.
- Security: WordPress database table prefix, debug mode status, presence of exposed sensitive files, authentication vulnerabilities.
- Speed/Core Web Vitals signals (beyond server response time).
- Google Search Console verification status.

## Top technical fixes (analysis only — NOT executed)
1.  Investigate and optimize Hostinger CDN caching strategy, particularly for static assets, to ensure `x-hcdn-cache-status` is not consistently DYNAMIC where static content should be cached.
2.  Review WordPress REST API security, considering rate limiting or authentication for specific endpoints, to mitigate potential abuse.